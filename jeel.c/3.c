#include<stdio.h>

int main()
{
 float radius,area;
  printf("enter radius of circle(in cm):");
  scanf("2f",&radius);
  area=3.14*radius*radius; 
  printf("area of circle=%cm\n",radius,area);  
    return 0;
}